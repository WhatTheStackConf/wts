import { ComingSoon } from "~/components/ComingSoon";

export default function Agenda() {
  return (
    <ComingSoon
      title="Agenda"
      subtitle="Full schedule with all tracks and sessions — coming after speaker confirmations."
    />
  );
}
